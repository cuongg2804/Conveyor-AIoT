.sider
  .inner-menu
    ul
      li
        a(href="/dashboard") Tổng quan
      li
        a(href="/history") Lịch sử kiểm tra
      if user && user.role === "ADMIN"
        li
          a(href="/logs") Nhật ký hệ thống
      if user && user.role === "ADMIN"
        li
          a(href="/conveyors") Băng tải
      if user && user.role === "ADMIN"
        li
          a(href="/cameras") Camera
      if user && user.role === "ADMIN"
        li 
          a(href="/models") Model AI
      if user && user.role === "ADMIN"
        li
          a(href="/users") Quản lý tài khoản
      if user && user.role === "ADMIN"
        li.has-submenu
          a(href="/#") Chế độ kiểm thử
          ul.submenu 
            li 
              a(href="/test-mode/settings") Cài đặt
            li
              a(href="/test-mode/history") Lịch sử kiểm thử

